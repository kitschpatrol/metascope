from setuptools import setup, find_packages

setup(
    name="chat-data-mcp-tools-ui",
    version="0.1.0",
    description="Chat Data MCP Tools UI",
    packages=find_packages(),
    python_requires=">=3.12",
    install_requires=[
        # Dependencies will be installed from requirements.txt
    ],
)
